export default function ChargerPage({ params }: { params: { serialNumber: string } }) {
    const serialNumber: string = params.serialNumber;
    const data = {
        
    };
    return (
        <>
            {serialNumber}
        </>
    );
};